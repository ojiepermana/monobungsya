# Angular SDK

This package contains generated client artifacts derived from `apps/gateway/erp/openapi.yaml`.

Run `bun run openapi:generate` after changing a public gateway schema. Generated files under `src/generated` are not edited manually.
