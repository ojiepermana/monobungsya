# Directory notes

OpenAPI fragments owned by each service are generated by `bun run openapi:generate`.

Keep domain contracts close to the service that owns them. Only the API gateway specification is public to external clients.
